<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.react.pnld.mappers.TrainingIndicatorMapper">

    <cache/>

    <resultMap id="trainingInstitutionData" type="com.react.pnld.dto.TrainingIndicatorInstitutionData">
        <result property="year" column="anno_capacitacion" />
        <result property="institutionNumberPNLD" column="cantidad_colegio" />
        <result property="percentageInstitutions" column="colegio_porcentaje"/>
        <result property="firstTimeInstitutionNumber" column="primera_vez_porcentaje"/>
    </resultMap>

    <resultMap id="trainingIndicatorInstitution" type="com.react.pnld.dto.TrainingIndicatorDTO">
        <id property="id" column="id_region"/>
        <result property="regionName" column="nombre_region" />
        <collection property="trainingIndicatorDataList" resultMap="trainingInstitutionData"/>
    </resultMap>

    <select id="trainingInstitutionData" resultMap="trainingIndicatorInstitution">
        SELECT
            d.anno_capacitacion,
            COUNT(d.rbd_colegio) AS cantidad_colegio,
            r.id AS id_region, r.nombre AS nombre_region,
            SUM
                (CASE WHEN d.rbd_colegio IN
                    (SELECT col.rbd
                        FROM pnld.colegio AS col
                        JOiN pnld.docente AS doc
                        ON col.rbd = doc.rbd_colegio
                        GROUP BY col.rbd
                        HAVING COUNT(DISTINCT doc.anno_capacitacion) = 1)
                THEN 1
                ELSE 0
            END)
            AS primera_vez_porcentaje
        FROM pnld.docente AS d
        JOIN pnld.colegio AS c ON d.rbd_colegio = c.rbd
        JOIN pnld.region AS r ON c.id_region = r.id
        WHERE d.anno_capacitacion BETWEEN #{fromYear} and #{toYear} GROUP BY r.id, d.anno_capacitacion ORDER BY r.id
    </select>

    <resultMap id="trainingTeacherData" type="com.react.pnld.dto.TrainingIndicatorTeacherData">
        <id property="rut" column="rut"/>
        <result property="year" column="anno_capacitacion" />
        <result property="gender" column="genero" />
        <result property="trainingState" column="capacitacion_aprobada"/>
        <result property="assistanceState" column="participo_en_pnld"/>
    </resultMap>

    <resultMap id="trainingIndicatorTeacher" type="com.react.pnld.dto.TrainingIndicatorDTO">
        <id property="id" column="id_region"/>
        <result property="regionName" column="nombre_region" />
        <collection property="trainingIndicatorDataList" resultMap="trainingTeacherData" javaType="List"/>
    </resultMap>

    <select id="trainingTeacherData" resultMap="trainingIndicatorTeacher">
        SELECT d.anno_capacitacion, d.capacitacion_aprobada, d.participo_en_pnld, g.tipo AS genero, r.id AS id_region, r.nombre AS nombre_region
        FROM
            pnld.docente AS d
            JOIN
            pnld.genero AS g
                ON d.id_genero = g.id
            JOIN
            pnld.colegio AS c
                ON d.rbd_colegio = c.rbd
            JOIN
            pnld.region AS r
                ON c.id_region = r.id
        WHERE d.anno_capacitacion BETWEEN #{fromYear} AND #{toYear}
        ORDER BY r.id asc
    </select>

</mapper>